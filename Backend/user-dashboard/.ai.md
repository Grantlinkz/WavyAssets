## Application Building Context — WavyAssets Sovereign Backend User Dashboard

Read the following files in order before implementing or making any architectural decision:

1. [`.ai/agents.md`](.ai/agents.md) — AI agent persona (Principal Backend Systems & Quantitative Financial Engineer), approved tools, and execution rules
2. [`.ai/project-overview.md`](.ai/project-overview.md) — product definition, goals, features, 7 sovereign asset verticals, and scope
3. [`.ai/architecture.md`](.ai/architecture.md) — modular NestJS 11 structure, Prisma schema, storage model, and invariants
4. [`.ai/code-standards.md`](.ai/code-standards.md) — TypeScript standards, DTOs, transactions, and comprehensive two-tier error handling
5. [`.ai/security.md`](.ai/security.md) — threat model, access control, secrets, 48-hour time-lock, and security invariants
6. [`.ai/ai-workflow-rules.md`](.ai/ai-workflow-rules.md) — 6-sprint development roadmap, human-in-the-loop, and delivery approach
7. [`.ai/ui-context.md`](.ai/ui-context.md) — API data contracts, command bar aggregate response, and custom error envelopes
8. [`.ai/progress-tracker.md`](.ai/progress-tracker.md) — current phase, completed work, open questions, and next steps

Also consult [`GEMINI.md`](GEMINI.md) for master system governance and [`tools/IMPLEMENTATION_STRATEGY.md`](tools/IMPLEMENTATION_STRATEGY.md) for technical blueprints.

---

## Non-Negotiable Invariants

1. **Adherence to Blueprint**: Never implement or alter endpoints, schemas, or features outside of defined specifications in `tools/IMPLEMENTATION_STRATEGY.md` without explicit approval.
2. **Ledger Zero-Sum Conservation**: For every transaction, $\sum \text{Debits} + \sum \text{Credits} = 0$. No user balance may ever be adjusted without a corresponding balanced ledger entry.
3. **48-Hour Quarantine Enforcement**: Any withdrawal to a destination with `status === 'QUARANTINE'` or `NOW() < quarantineUntil` must be rejected immediately at the database query level.
4. **Zero Plaintext Secrets**: Passphrases (Argon2id), session tokens (HMAC-SHA256), PII/Card Data (AES-256-GCM). Never log or return sensitive credentials.
5. **Comprehensive Two-Tier Error Handling**:
   - **Code-Based**: Every service and controller must throw semantic NestJS HTTP exceptions (`BadRequestException`, `NotFoundException`, `ForbiddenException`, etc.) or custom domain exceptions.
   - **Global Filter**: `GlobalExceptionFilter` must intercept all uncaught exceptions, sanitize payloads, and return a standardized custom unified JSON response envelope without leaking stack traces or database internals.
6. **Redacted Logging**: All emitted logs must redact authorization headers, access tokens, email addresses, passphrases, and account numbers.
7. **Continuous Testing**: Unit and Integration tests must be updated after every phase or step, arranged strictly in dedicated directories (`Tests/UnitTest/<test-name>` and `Tests/IntegrationTest/<test-name>`).
8. **Documentation Updates**: Docs must be updated after every phase or step — keep context files, progress tracking, and code in strict lockstep before declaring work complete.

---

## Human-in-the-Loop Rules

- Always propose an implementation plan or prompt in `prompts/<sprint-name>-<unit-name>.md` and obtain user approval before writing code.
- Stop and prompt for human review whenever encountering ambiguity, architectural pivots, or destructive actions.
- Provide clear verification steps, automated test outputs, and proof of correctness after completing any unit of work.

---

## Git Commit Standards

- **Git Commit Frequency**: Commit code as you build. Every working session or phase must contain **at least two git commits**.
- **Conventional Commit Patterns**: All git commit messages must strictly adhere to the following prefixes:
  - `feat:` for new features (e.g. `feat: implement double-entry ledger journal balance check`)
  - `fix:` for bug fixes (e.g. `fix: resolve 48-hour quarantine datetime comparison bug`)
  - `refactor:` for code refactoring (e.g. `refactor: clean up command-bar aggregation pipeline`)
  - `docs:` for documentation updates (e.g. `docs: update progress-tracker and architecture context`)
  - `tests:` for tests addition and modification (e.g. `tests: add Vitest suite for time-lock rejection`)
  - `chore:` for maintenance tasks and environment setup (e.g. `chore: configure Prisma schema and seed data`)

---

## Pre-Commit Rules

Before committing code, submitting changes, or marking a task complete:

1. Run and pass all static analysis checks (`npm run lint`).
2. Run strict TypeScript typechecks (`npx tsc --noEmit`) ensuring zero errors.
3. Ensure Unit and Integration tests are updated and passing in `Tests/UnitTest/<test-name>` and `Tests/IntegrationTest/<test-name>`.
4. **Error Handling & Logging**: Verify that all new pathways have comprehensive error handling and secure, redacted logging.
5. Update docs and `.ai/progress-tracker.md` with completed items, session notes, and architectural updates after every phase or step.
